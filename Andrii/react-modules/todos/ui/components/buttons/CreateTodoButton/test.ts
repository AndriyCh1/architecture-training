import { getTodoStore } from "#modules/todos/data/store/instance";
import { CreateTodoButton } from "#modules/todos/ui/components/buttons/CreateTodoButton";

describe(CreateTodoButton.name, () => {
  test("render", () => {
    mock(getTodoStore, () => {}
  })
})

